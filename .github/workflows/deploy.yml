name: Deploy Flutter Web to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.6"
          channel: stable
          cache: true

      - name: Install Dependencies
        run: flutter pub get

      - name: Build Flutter Web
        run: flutter build web --release

      - name: Upload Web Files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 168.231.121.19
          username: root
          password: ${{ secrets.VPS_PASSWORD }}
          source: "build/web/*"
          target: "/tmp/lactosure_web"
          strip_components: 1

      - name: Deploy to Nginx
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 168.231.121.19
          username: root
          password: ${{ secrets.VPS_PASSWORD }}
          script: |
            rm -rf /var/www/lactosure_web/*
            cp -r /tmp/lactosure_web/* /var/www/lactosure_web/
            chown -R www-data:www-data /var/www/lactosure_web
            systemctl reload nginx
            echo "Flutter Web deployed successfully!"